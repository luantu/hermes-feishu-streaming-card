# V4.6.1：Hermes 0.21.3 兼容与通知修复

- #326：工具回调 hook 选择必经赋值位置，避开 `mute_notification_reply` 的后置条件重置；普通对话恢复工具事件，静默对话仍保持静默。只有条件分支或嵌套函数中的未知布局不会被当作有效锚点。
- #325：状态提示保留纯文本；只撤回已识别的临时状态模板，普通答案、排队确认、维护拒收和错误说明不会因为带有“⏳”而被删除。撤回保留 profile/chat/thread，拒绝跨聊天或无效上下文。
- 重启完成提示使用原生文本，发送失败保留原生 fallback。独立通知不会抢占已有会话的 alias 或正文卡。
- 已发送独立审批卡时，会话卡不重复展示按钮；专用审批卡及决定后的结果保持可见。
- 工具区按执行顺序保留最近两步，所有仍在执行的工具都可见。撤回诊断记录时间、已验证的 API 错误码和哈希标识，不记录原始异常正文或 HTTP 请求路径。中断及引导当前任务确认从真实 event profile 安排撤回，附带首次使用指引的消息保留。
- 正文思考按时间正序，工具面板继续最新优先；没有统计数据时不显示虚构的 Unknown/零值行，页脚不重复完成通知。

## 验证与边界

上游稳定版 `345cd2b057a452236de401d3534b8502a7465e8d` 与后续仍标为 0.21.3 的源码布局分别验证。#326 在上游 `c62bd9f2078a946108f1c9d9b24bf118963277ef` 的实际回调方法上复现：4.6.0 普通分支无工具事件，修复后开始/结束各一次，静默分支不投递。

固定源码升级、重复安装、逐字卸载还原，全量测试、跨平台 CI、精确合并提交、三平台资产校验和及公开 tag 安装均为发布门禁。最终证据在发布时补充；模拟投递不冒充飞书手机真机验收。4.6.0 的展示恢复边界不变，不恢复执行栈或旧审批。

## 贡献

感谢 [mouyong](https://github.com/mouyong) 的 [#326](https://github.com/baileyh8/hermes-feishu-streaming-card/issues/326) 现场定位及 [PR #325](https://github.com/baileyh8/hermes-feishu-streaming-card/pull/325)（截至 `bf7409a`，保留原始提交与作者）。维护者补充 profile 隔离、撤回内容边界、失败 fallback 和未知控制流回归。
