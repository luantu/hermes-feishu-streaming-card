# Issues #196/#197/#199 and v4.2.9 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:executing-plans` to implement this plan task by task.

**Goal:** Securely integrate the useful interaction work from PRs #196 and #199, fix quoted-card answer summaries from Issue #197, and publish the next verified patch release.

**Architecture:** Keep the Hermes hook fail-open, but make native slash confirmation claims atomic before handing work to the Gateway loop. Keep interaction callbacks bound to an unguessable callback token and an exact non-empty chat identity, including schema-2 form submissions. Build Feishu card `config.summary` from a bounded normalized answer excerpt at terminal state so quoted messages retain useful context.

**Tech Stack:** Python 3.9+, aiohttp, Feishu/Lark card schema 2.0, pytest/pytest-asyncio, GitHub Actions.

---

### Task 1: Freeze a trustworthy baseline

**Files:**
- Create: `docs/superpowers/plans/2026-08-09-issues-196-199-v4.2.9.md`

- [x] Create an ignored worktree from exact `origin/main`.
- [x] Use a worktree-local runtime environment so `python -I` imports the same candidate version.
- [x] Run `.venv/bin/python -m pytest -q` outside the sandbox for loopback-only fixtures.
- [x] Record the green baseline: 2431 passed, 6 skipped.

### Task 2: Secure PR #196 native slash confirmation

**Files:**
- Modify: `hermes_feishu_card/hook_runtime.py`
- Modify: `tests/unit/test_hook_runtime.py`

- [x] Add failing tests proving a callback is not lost when loop submission returns false or raises.
- [x] Add a failing test proving two distinct deliveries cannot resolve one pending confirmation twice.
- [x] Integrate the contributor commit while preserving authorship.
- [x] Claim pending state atomically before scheduling; restore it or execute a synchronous fallback if scheduling is not accepted.
- [x] Run the focused hook runtime and SDK compatibility tests.

### Task 3: Secure and integrate PR #199 interaction forms

**Files:**
- Modify: `hermes_feishu_card/render.py`
- Modify: `hermes_feishu_card/server.py`
- Modify: `hermes_feishu_card/session.py`
- Modify: `hermes_feishu_card/hook_runtime.py`
- Modify: `tests/unit/test_render.py`
- Modify: `tests/unit/test_hook_runtime.py`
- Modify: `tests/integration/test_server.py`

- [x] Add failing tests for multi-select plus free-text form flow.
- [x] Add failing security tests for missing/wrong callback token and missing/wrong chat identity.
- [x] Integrate the contributor commits while preserving authorship.
- [x] Put the callback token in the form action identity and require exact token plus exact non-empty chat binding.
- [x] Preserve pending-card freeze, read-only confirmation before text fallback, and timeout footer behavior.
- [x] Run focused renderer, session, hook runtime, server, and SDK compatibility tests.

### Task 4: Fix Issue #197 quoted-card context

**Files:**
- Modify: `hermes_feishu_card/render.py`
- Modify: `tests/unit/test_render.py`
- Modify: `tests/integration/test_server.py`

- [x] Add a failing test showing a completed card summary contains a normalized answer excerpt instead of only the fixed status.
- [x] Keep non-terminal summaries status-based and bound the terminal excerpt.
- [x] Verify summary lookup still returns the same bounded user-visible answer context.

### Task 5: Prepare and verify v4.2.9

**Files:**
- Modify: `pyproject.toml`
- Modify: `hermes_feishu_card/__init__.py`
- Modify: `CHANGELOG.md`
- Create: `docs/release-notes-v4.2.9.md`
- Modify: `README.md`
- Modify: `README.en.md`
- Modify: affected files under `docs/wiki/`

- [x] Update version contracts and user-facing documentation.
- [x] Run focused tests, then `.venv/bin/python -m pytest -q`.
- [x] Run `git diff --check`, build wheel/sdist, and verify package metadata/import provenance.
- [x] Push the integration branch and require green GitHub Actions.

### Task 6: Merge, release, and close the public queue

**Files:**
- Verify: exact `main` merge SHA in a clean detached worktree.
- Verify: GitHub Release assets and checksums.

- [ ] Merge the verified branch to `main`.
- [ ] Re-run the full suite and `git diff --check` on the exact merge SHA.
- [ ] Create and push annotated tag `v4.2.9`.
- [ ] Confirm release-assets workflow, wheel, sdist, archives, and checksums.
- [ ] Reinstall from the public tag/Release and prove import from isolated `site-packages`.
- [ ] Update and close resolved, superseded, insufficient-evidence, or out-of-scope Issues/PRs with precise reasons and contributor credit.
- [ ] Re-audit the remaining open Issues/PRs.
