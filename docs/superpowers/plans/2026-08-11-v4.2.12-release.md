# V4.2.12 Release Implementation Plan

> **Execution mode:** Inline in the current Codex task. Bailey already authorized the release.

**Goal:** Publish `v4.2.12` from `origin/main@5e9787a8a46cb6cc0bee39b173643cb0fa60cb3d`, containing PR #206 approval-card capability enforcement and PR #205 stable zero-tool timeline rendering.

**Scope boundaries:** Keep the original worktree untouched, do not modify `legacy/`, do not patch or restart the installed Hermes runtime, do not send a new real Feishu message, and do not tag until the exact merged release commit passes the full suite and package provenance checks.

## Task 1: Release metadata and public documentation

**Files:**

- Modify: `pyproject.toml`
- Modify: `hermes_feishu_card/__init__.py`
- Modify: `.github/workflows/tests.yml`
- Modify: `config.yaml.example`
- Modify: `docker-compose.example.yml`
- Modify: `README.md`
- Modify: `README.en.md`
- Modify: `README-install.md`
- Modify: `CHANGELOG.md`
- Modify: `TODO.md`
- Modify: `docs/user-guide.md`
- Modify: `docs/user-guide.en.md`
- Modify: `docs/release-readiness.md`
- Modify: `docs/release-readiness.en.md`
- Modify: `docs/wiki/feishu-acceptance.md`
- Create: `docs/release-notes-v4.2.12.md`
- Create: `docs/release-notes-v4.2.12.en.md`
- Modify: `tests/unit/test_docs.py`
- Modify: `tests/unit/test_package_metadata.py` only if its version contract is not metadata-driven

**Steps:**

1. Change current-version defaults from `4.2.11` / `v4.2.11` to `4.2.12` / `v4.2.12` without rewriting historical release notes.
2. Add bilingual release notes describing both merged PRs, approval safety boundaries, zero-tool timeline behavior, upgrade commands, expected assets, and the explicit real-Feishu verification boundary.
3. Add current release entries to README, CHANGELOG, TODO, user guides, readiness records, and the Feishu acceptance matrix.
4. Add a docs contract test for the V4.2.12 release files and behavior markers; update current-version assertions mechanically.
5. Run:

   ```bash
   python -m pytest tests/unit/test_docs.py tests/unit/test_package_metadata.py -q
   git diff --check
   ```

## Task 2: Candidate verification and package provenance

**Steps:**

1. Run focused runtime/render coverage:

   ```bash
   python -m pytest tests/unit/test_hook_runtime.py tests/unit/test_session.py tests/unit/test_render.py tests/integration/test_server.py -q
   ```

2. Run the complete candidate suite and whitespace gate:

   ```bash
   python -m pytest -q
   git diff --check
   ```

3. Build sdist and wheel with the bundled workspace Python/build runtime.
4. Install the regular wheel into a fresh temporary venv outside the checkout.
5. Assert package and distribution versions are `4.2.12`, module import originates from `site-packages`, and CLI help exits successfully.

## Task 3: PR and CI

**Steps:**

1. Commit only the release files with message `docs: prepare v4.2.12 release`.
2. Push `codex/v4.2.12-release` and create a ready PR against `main`.
3. Wait for Python 3.9–3.12, macOS, Windows, Docker Compose, PowerShell installer, Feishu SDK compatibility, and CodeQL checks.
4. Merge with a merge commit only when every required check passes and the PR head SHA is unchanged.

## Task 4: Exact-merge verification and tag

**Steps:**

1. Fetch the merged `main` and create a detached worktree at the exact merge SHA.
2. Re-run the complete suite and `git diff --check` at that SHA.
3. Rebuild sdist/wheel and repeat the clean `site-packages` provenance smoke.
4. Create an annotated `v4.2.12` tag pointing to that exact verified merge SHA.
5. Push the tag only after verifying its peeled commit.

## Task 5: Public Release verification

**Steps:**

1. Wait for `release-assets.yml` to succeed at the tagged merge SHA.
2. Confirm the non-draft, non-prerelease GitHub Release and its four expected assets.
3. Download the public assets and run the published SHA-256 checksums.
4. Install from public GitHub tag `v4.2.12` in a second fresh venv and assert `site-packages` provenance plus CLI help.
5. Replace the generated Release body with `docs/release-notes-v4.2.12.md` if the workflow did not already use it.
6. Recheck `origin/main`, tag peel, Release metadata, workflow SHA, open PRs, and the untouched original worktree before reporting completion.
