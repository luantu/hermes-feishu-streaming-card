# v4.5.2：排队结果与临时提示修复

整合 [mouyong](https://github.com/mouyong) 的 [PR #310](https://github.com/baileyh8/hermes-feishu-streaming-card/pull/310) 截至 `d979c46`，保留原提交作者，并补充维护者回归保护。

- #318：排队任务完成 hook 传递执行结果，失败时保留已流出的正文并追加错误，不再走成功分支归档正文。新版 Hermes 优先采用最终整理后的 `_delivery_result`，旧布局仍使用 `result`；覆盖两者成功/失败不一致的情况。
- #321：成功新发的 Working 心跳与 Redirected 确认提示可在 15 秒后撤回。只从受约束的通知调用位置触发；失败发送、普通答案引用、未知平台和无消息 ID 不安排撤回，原有正文卡片保护继续生效。编辑成功不重新安排撤回。
- 心跳 patch 必须唯一匹配已知异步通知函数、成功分支及紧邻的新发送语句；重复、未知或改变内容来源的锚点保持不修改。覆盖 LF/CRLF、重复安装、逐字卸载和实际执行路径。
- 工具活动序号排在耗时之前；具体动作放在标题下一行。审批和完成状态保留自己的标题说明，目标/参数沿用脱敏及长度限制。

验证范围：真实固定 Hermes 源码锚点与安装回归、会话/渲染/服务端测试、跨平台 CI 与公开 tag 安装；真实手机展示和原报告环境的重启全过程不以自动化代替。

#282 按报告者“不再追踪”关闭，不算已验证修复。#319 中间思考缺失、#320 重启后恢复消息走普通文本仍待环境与日志证据，不包含在本版已修复结论中。本轮未升级生产。
